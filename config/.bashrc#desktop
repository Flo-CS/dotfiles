source ~/.dotfiles/config/.bashrc

. "$HOME/.atuin/bin/env"
[[ -f ~/.bash-preexec.sh ]] && source ~/.bash-preexec.sh
eval "$(atuin init bash)"

. "$HOME/.cargo/env"
export PATH="$HOME/development/flutter/bin:$PATH"
