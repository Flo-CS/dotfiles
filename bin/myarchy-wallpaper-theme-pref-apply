#!/usr/bin/env bash

wallpaper_name=$(cat $MYARCHY_DIR/files/themes/current/wallpaper.theme)

if [ -z "$wallpaper_name" ]; then
	echo "No wallpaper name found in the current theme."
	exit 1
fi

myarchy-wallpaper-set "$wallpaper_name"
