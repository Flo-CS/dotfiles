#!/usr/bin/env bash

BIN_DIR="$HOME/.local/bin"
DESKTOP_DIR="$HOME/.local/share/applications"
ICON_DIR="$DESKTOP_DIR/icons"

get_all_appimages() {
    find "$DESKTOP_DIR" -name '*.desktop' -print0 |
        while read -r -d '' file; do
            exec_line=$(grep "^Exec=" "$file" | cut -d= -f2)
            if echo "$exec_line" | grep -q "$BIN_DIR"; then
                basename "${file%.desktop}"
            fi
        done | sort
}

remove_appimage() {
    local app="$1"

    rm -f "$BIN_DIR/$app"
    rm -f "$DESKTOP_DIR/$app.desktop"
    rm -f "$ICON_DIR/$app.png"

    echo "Removed $app"
}

if [ "$#" -eq 0 ]; then
    appimages=$(get_all_appimages)

    if [ -z "$appimages" ]; then
        echo "No AppImages to remove."
        exit 1
    fi

    selected=$(echo "$appimages" | gum choose --no-limit --header "Select AppImage to remove..." --selected-prefix="✗ ")

    if [ -z "$selected" ]; then
        echo "No AppImage selected."
        exit 1
    fi

    echo "$selected" | while read -r app; do
        [ -n "$app" ] && remove_appimage "$app"
    done
else
    for app in "$@"; do
        remove_appimage "$app"
    done
fi
