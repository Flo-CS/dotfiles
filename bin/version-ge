#!/usr/bin/env bash

set -euo pipefail

version_ge() {
	[ "$1" = "$2" ] && return 0
	[ "$(printf '%s\n' "$1" "$2" | sort -V | head -n1)" != "$1" ]
}

if version_ge "$1" "$2"; then
	exit 0
else
	exit 1
fi
