#!/usr/bin/env bash

wallpaper_name=$(cat $DOTS_DIR/themes/current/wallpaper.theme)

if [ -z "$wallpaper_name" ]; then
	echo "No wallpaper name found in the current theme."
	exit 1
fi

dots-wallpaper-set "$wallpaper_name"
